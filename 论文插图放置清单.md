# 技术论文插图放置清单

本文档对应 `技术论文_3.3.5后修改版.docx`，重点列出 `3.3.5 外部存储电路` 之后建议补充的图片。原文 `3.3.5` 及之前已有图片不在本清单中重复列出。

| 图号 | 建议放置位置 | 图名 | 图片内容要求 | 建议来源 |
|---|---|---|---|---|
| 图3.12 | 3.4 FCU主控板硬件实物与接口说明，第1段后 | FCU主控板实物图 | 展示主控板整体实物，尽量包含板卡正面、主要芯片、接口、安装孔和电源端子。 | 拍摄主控板实物照片 |
| 图3.13 | 3.4 FCU主控板硬件实物与接口说明，第2段后 | FCU主控板接口与功能分区图 | 在主控板实物或PCB图上标注STM32主控区、DCL供电区、阀体驱动区、风扇控制区、ADC采集区、CAN/RS485通信区、SWD调试口。 | 在实物图或PCB图上加标注 |
| 图4.1 | 4.1 软件总体架构，第1段后 | FCU软件分层架构图 | 自下而上展示驱动层、控制层、应用层；标出ADC/PWM/RS485/风扇/阀体/DCDC驱动、状态机、通信解析、故障保护和数据上报。 | 根据固件模块绘制流程/框图 |
| 图4.2 | 4.2 主程序状态机设计，第1段后 | FCU主程序状态机流程图 | 包含SYS_INIT、SYS_CALIBRATION、SYS_WAIT_POWER、SYS_SOFT_START、SYS_RUNNING、SYS_FAULT、SYS_SHUTDOWN、SYS_POWER_LOSS等状态及跳转条件。 | 根据 `User/main.c` 绘制 |
| 图4.3 | 4.3 数据采集与标定程序设计，第1段后 | ADC采集与零点标定流程图 | 展示ADC扫描、DMA搬运、影子缓存、移动平均滤波、压力换算、NTC温度换算、电流零点校准和补偿输出。 | 根据 `Hardware/bsp_adc.c` 绘制 |
| 图4.4 | 4.4 DCF闭环控制与软启动策略，第1段后 | DCF闭环控制框图 | 展示目标电压/目标电流、采样反馈、电压环/电流环PID、占空比限幅、PWM输出和功率级之间的关系。 | 根据 `Hardware/bsp_dcdc_boost.c` 绘制 |
| 图4.5 | 4.4 DCF闭环控制与软启动策略，第2段后 | DCF软启动流程图 | 展示启动命令、软启动占空比递增、输入/输出检测、故障判断、进入运行或停机保护的流程。 | 根据 `User/main.c` 和 `bsp_dcdc_boost.c` 绘制 |
| 图4.6 | 4.5 执行机构控制策略，第1段后 | 进排氢阀与风扇控制逻辑图 | 展示控制帧标志位如何映射到进氢阀、排氢阀、风扇高边供电、风扇PWM占空比和FG转速反馈。 | 根据 `bsp_valve.c`、`bsp_fan.c` 绘制 |
| 图4.7 | 4.6 通信协议与信息管理，第1段后 | RS485固定帧格式图 | 展示16字节帧结构：AA 55帧头、4字节ID、LEN、8字节DATA、SUM校验，并说明沿用CAN扩展帧ID语义。 | 根据 `SERIAL_PROTOCOL_FOR_MCU.md` 绘制 |
| 图4.8 | 4.6 通信协议与信息管理，第2段后 | FCU与上位机数据交互图 | 展示FCU周期上报4类状态帧，上位机下发0x18FF10A0控制帧，形成状态监测与控制闭环。 | 根据通信协议文档绘制 |
| 图4.9 | 4.7 告警与保护模块，第1段后 | FCU故障保护流程图 | 展示输入欠压/过压、输出过压/过流、短路、急停触发后PWM清零、功率级停机、故障码上报和复位恢复流程。 | 根据 `main.c`、`bsp_dcdc_boost.c` 绘制 |
| 图5.1 | 5.1 硬件接口调试，第1段后 | FCU硬件接口调试连接图 | 展示电源、电子负载或等效负载、USB-RS485、SWD调试器、压力/温度传感器、阀体和风扇的连接关系。 | 拍摄调试台架或绘制连接框图 |
| 图5.2 | 5.2 通信与上位机联调，第1段后 | FCU周期状态帧串口调试截图 | 展示AA55帧头、状态帧ID、心跳计数、电压电流/温度压力/外设状态帧的串口接收结果。 | 串口调试助手截图 |
| 图5.3 | 5.2 通信与上位机联调，第2段后 | 上位机控制帧下发测试截图 | 展示0x18FF10A0控制帧下发后，风扇占空比、阀体状态或系统状态反馈发生变化。 | 上位机或串口调试截图 |
| 图5.4 | 5.3 功率级与保护功能测试，第1段后 | DCF软启动测试曲线 | 展示软启动过程中输出电压、输出电流和PWM占空比随时间变化的曲线。 | 示波器、数据记录或测试数据绘图 |
| 图5.5 | 5.3 功率级与保护功能测试，第2段后 | 故障保护测试结果图 | 展示模拟过压、过流、短路或急停后PWM清零、系统进入故障状态、故障码上报的测试结果。 | 示波器截图、上位机截图或测试数据绘图 |

## 优先补图建议

优先级最高的是图3.12、图3.13、图4.2、图4.7和图5.2。它们分别对应硬件实物、接口分区、软件主流程、通信协议和联调结果，是答辩或评审时最容易被追问的证据图。

图4.4、图4.5和图5.4建议在有DCF调试数据后补充，避免把需求目标或理论曲线写成实测结果。
